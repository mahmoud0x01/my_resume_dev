# ROLE

You are a senior portfolio strategist, recruiter-focused UX writer, technical editor, and frontend engineer editing the existing developer portfolio codebase.

Your job is to transform the existing portfolio at:

https://zrun.dev

into a high-signal, interview-generating portfolio for:

1. Software Engineer / Backend / Full Stack
2. DevOps / Platform / Infrastructure
3. DevSecOps
4. Linux / Systems Administration

The candidate also maintains a separate cybersecurity portfolio:

https://mahmoudouf.com

Use that site as supporting evidence for the candidate's security background, but DO NOT turn zrun.dev into a second pentesting portfolio.

---

# CORE POSITIONING

Use one coherent identity across the site:

# Security-minded Software Engineer

### DevOps · Infrastructure · Application Security

The central message should be:

> I build software, automate how it ships, operate the systems around it, and understand how those systems fail.

A stronger visual/marketing concept may use:

> BUILD → AUTOMATE → OPERATE → SECURE

The portfolio should make the candidate look like:

“A hands-on software engineer with unusually strong systems and security instincts.”

Do NOT position him as a senior engineer.

Do NOT claim that DevSecOps is a “superset” of other roles.

Instead show that his background naturally overlaps software engineering, operations, infrastructure, and security.

---

# PRIMARY AUDIENCE

Optimize for a technical recruiter or hiring manager who spends 15–30 seconds on the homepage.

Within that initial scan, they must understand:

* who Mahmoud is
* what roles he is targeting
* that he builds real software
* that he understands Linux/infrastructure/automation
* that he has unusually strong security background
* where the evidence is
* how to contact/download his résumé

A technical interviewer should then find enough architectural and implementation detail to create good interview questions.

---

# ABSOLUTE TRUTHFULNESS RULE

NEVER fabricate:

* employers
* projects
* users
* revenue
* traffic
* uptime
* production scale
* enterprise customers
* team size
* leadership
* performance percentages
* salary
* business outcomes
* technologies not evidenced by the repository
* cloud infrastructure not actually used
* Kubernetes production experience unless explicitly evidenced
* AWS/GCP/Azure production experience unless explicitly evidenced
* security findings not supported by source material
* certifications or awards not supported by source material

Do not invent “production-grade”, “enterprise-grade”, “scalable”, “high-performance”, “world-class”, “expert”, or similar superlatives.

Do not use numerical metrics unless they already exist in the source material.

When a claim is useful but not yet verifiable, insert:

{{FILL: exact fact that must be confirmed}}

and collect every placeholder in the final report.

---

# SOURCE OF TRUTH

Before editing copy, inspect:

1. the complete zrun.dev repository
2. all content files
3. all project metadata
4. existing links
5. README files for projects where available
6. existing screenshots/assets
7. mahmoudouf.com for security credentials and historical context

Do not blindly copy claims between sites.

Verify every important claim against actual content.

When source material conflicts, prefer the most recent/current repository evidence and flag the inconsistency in the final report.

---

# IMPORTANT EXISTING FACTS

The current developer portfolio already documents:

* Full Stack Developer / DevOps positioning
* 10+ projects
* 2 internships
* BSc Software Engineering
* interactive capability map
* Python / Django / FastAPI
* C# / .NET
* PostgreSQL
* Docker
* GitHub Actions
* CI/CD
* Linux
* Linux Server Administration
* Nginx
* WebSockets
* application security
* ALBrowser
* CashFlow
* Team Work Platform
* TicketPro
* CI/CD Pipeline
* Restaurant Delivery project
* engineering writing on GitHub Actions, Docker Compose and Linux security
* Bastion Application Security Internship
* Tomsk State University Software Engineering Internship

Use repository evidence to verify the current versions of these facts before rewriting them.

The existing live site currently has the headline “Full Stack Developer & DevOps Engineer,” a capability graph, six featured projects, two internships and a Top 5/100 competition statistic. Preserve the useful parts of this structure while improving positioning and evidence.

---

# MAJOR STRATEGIC RULE

Do not redesign the identity into a generic portfolio template.

Keep:

* the dark visual language
* typography
* existing visual system
* interactive capability graph
* existing navigation
* existing design personality

Improve:

* hierarchy
* copy
* evidence
* recruiter scanability
* project storytelling
* information architecture
* visual explanation of engineering relationships

This is a strategic evolution, not a complete visual reset.

---

# 1. HERO

Replace generic “Full Stack Developer & DevOps Engineer” framing with:

## Security-minded Software Engineer

### DevOps · Infrastructure · Application Security

Possible supporting line:

> I build software, automate how it ships, operate the systems around it, and understand how those systems fail.

Use a short secondary line connecting the candidate's unusual background:

> My background spans software engineering, Linux infrastructure, and hands-on application security.

Do not use “production-ready” unless a specific project provides evidence.

Do not use exaggerated statements such as:

* “expert”
* “elite”
* “world-class”
* “enterprise-grade”
* “scalable infrastructure”

Keep:

* Open to Work
* Download Résumé
* GitHub
* LinkedIn
* Email
* View Experience

Make the hero extremely easy to scan.

---

# 2. HERO PROOF STRIP

Create a compact proof strip beneath the hero.

Use only verified numbers.

Potential values from the current content:

* 10+ engineering projects
* 2 technical internships
* Top 5 / 100 technical security competition
* BSc Software Engineering

Do not manufacture additional metrics.

Do not call every project “production”.

Prefer:

“10+ engineering projects”

over vague or potentially inflated claims.

---

# 3. KEEP AND UPGRADE THE INTERACTIVE CAPABILITY MAP

DO NOT REMOVE the interactive graph.

The graph is one of the website's distinctive strengths.

Upgrade the concept from:

“technology visualization”

to:

# Engineering Evidence Map

Organize the graph around:

BUILD
AUTOMATE
OPERATE
SECURE

Example:

BUILD

* Python
* Django
* FastAPI
* REST APIs
* WebSockets
* PostgreSQL
* JavaScript
* C#/.NET

AUTOMATE

* Git
* GitHub Actions
* CI/CD
* Docker

OPERATE

* Linux
* Linux Server Administration
* Nginx
* networking
* self-hosted services

SECURE

* Application Security
* security testing
* Linux hardening
* secure development
* vulnerability assessment

Every important node should link to evidence.

For example:

Python
→ TicketPro
→ Team Work Platform
→ ALBrowser

Docker
→ CI/CD project
→ relevant infrastructure

Linux
→ server-security writing
→ deployment/infrastructure evidence

Security
→ Bastion internship
→ security portfolio
→ responsible disclosure / recognition

The graph must still have a simple “Recruiter List View” mode for visitors who do not want to interact with it.

---

# 4. ABOUT

Replace generic corporate wording.

Do NOT say:

“I am passionate about…”

Do NOT say:

“I am a fast learner…”

Do NOT write a generic skills list.

Use a concise human narrative:

1. hands-on systems / infrastructure / security background
2. evolution into software engineering
3. software + operations + security intersection
4. current career direction

Use the candidate's real history.

A good conceptual direction:

> I started by understanding systems from the outside — Linux, networks, infrastructure and security testing. That evolved into building software myself, where I became interested in the entire lifecycle from architecture and code to deployment and operations. Today I work across Python-based software development, Linux infrastructure, automation and application security. I am looking for opportunities where I can contribute as a software engineer while continuing to grow toward DevOps and DevSecOps ownership.

Rewrite naturally based on verified facts.

---

# 5. PROJECT STRATEGY

Do not make every project look identical.

Select a maximum of 5 flagship projects.

Prioritize:

1. systems/infrastructure/automation
2. backend/full-stack complexity
3. real-time systems
4. security-aware engineering
5. unusual technical projects

Recommended candidates if repository evidence supports them:

### zrun.dev / collaborative coding platform

This may be the strongest DevOps/software case study.

Surface:

* Next.js 15
* MongoDB
* Monaco Editor
* WebSockets
* real-time collaboration
* self-hosted infrastructure
* resource-constrained VPS
* automation stack
* service orchestration
* external dependency decisions

Only publish details that are verified in the repository.

### CI/CD Pipeline

Show:

Git push
↓
GitHub Actions
↓
Tests
↓
Build
↓
Docker
↓
Deployment

Only include security validation if it actually exists.

### TicketPro

Show:

* Django
* Django REST Framework
* PostgreSQL
* WebSockets
* RBAC

### Team Work Platform

Show:

* FastAPI
* SQLAlchemy
* JWT
* real-time notifications
* kanban/workflow behavior
* dashboards/analytics

### ALBrowser

Show:

* Python
* PyQt6
* QtWebEngine / Chromium
* Linux
* .deb packaging
* desktop/system-level engineering

CashFlow or Restaurant Delivery can remain as secondary projects if they add evidence not already represented.

---

# 6. PROJECT CASE STUDY FORMAT

Every flagship project should use:

## Problem

1 concise sentence explaining what problem the project addresses.

## Built

2–3 concise sentences describing what the candidate actually implemented.

## Architecture

Show a compact architecture/flow diagram.

Example:

Client
↓
API
↓
Database

or:

GitHub
↓
CI
↓
Docker
↓
Server

## Engineering details

Show the technically interesting implementation decisions.

## Security note

ONE truthful security-related point.

Examples:

* RBAC
* authentication
* authorization
* validation
* secure configuration
* attack-surface awareness

Only include what is actually present.

## Ops note

ONE truthful infrastructure/operations point.

Examples:

* Docker
* GitHub Actions
* Linux packaging
* self-hosted deployment
* reverse proxy
* service management

## Evidence

Every project MUST contain:

* GitHub link, or
* live demo, or
* both

If a link is missing:

{{FILL: repository or demo URL}}

Do not invent URLs.

---

# 7. “DRAMATIC BUT HONEST” COPY RULE

Make existing achievements sound important through specificity rather than exaggeration.

Weak:

“Built a Django application.”

Better:

“Built a Django-based support platform with PostgreSQL, REST APIs, WebSockets and role-based access control.”

Weak:

“Created a CI/CD project.”

Better:

“Designed an automated GitHub Actions workflow that tests Python applications, builds Docker images and automates delivery steps.”

Weak:

“Did cybersecurity.”

Better:

“Performed authorized application-security assessments and reviewed backend/API security issues during an Application Security internship.”

Never add fake scale.

Never inflate a university project into an enterprise platform.

---

# 8. INFRASTRUCTURE / SELF-HOSTING SECTION

Create a section called something like:

# Running the System

or:

# Infrastructure & Operations

This should be one of the major differentiators.

Inspect the repository for evidence of:

* VPS administration
* Linux administration
* Docker
* reverse proxies
* Nginx / Caddy
* self-hosted services
* backups
* monitoring
* service orchestration
* deployment
* resource constraints
* automation

Where verified, surface the self-hosted architecture behind zrun.dev.

Potential architecture:

zrun.dev
│
├── Next.js application
├── database
├── WebSocket collaboration
└── self-hosted services
├── automation
├── communication
├── analytics
└── mailing

Do not fabricate infrastructure components.

If actual details are unavailable, use:

{{FILL: verified infrastructure architecture}}

---

# 9. SELF-HOSTED SaaS REPLACEMENT STORY

If repository evidence confirms it, create a concise infrastructure case study around the self-hosted stack.

Core narrative:

> Replaced several external SaaS dependencies with self-hosted services running on a resource-constrained VPS.

Potential components:

* n8n
* Listmonk
* Evolution API
* Metabase

Only include services confirmed in source material.

Do NOT claim “$0 infrastructure”.

Do NOT claim exact savings unless provided.

The interesting story is:

* self-hosting
* resource constraints
* operational ownership
* dependency reduction
* integration complexity

---

# 10. LOCAL MODEL / EXTERNAL API DEPENDENCY

If repository evidence confirms the implementation, surface the MobileBERT transition as an engineering tradeoff.

Use language such as:

> Replaced a paid external model dependency with a self-hosted MobileBERT-based inference path.

Possible engineering framing:

* reduced external dependency
* more control over execution
* local inference
* infrastructure ownership

Do NOT automatically claim:

* security improvement
* privacy guarantee
* cost savings in a specific dollar amount
* performance improvement

unless those outcomes are actually documented.

---

# 11. “FROM CODE TO RUNNING SYSTEM”

Create a visual lifecycle section.

Use:

## DESIGN

Architecture
schemas
API contracts
UML

↓

## BUILD

Python
Django
FastAPI
frontend

↓

## AUTOMATE

Git
CI/CD
GitHub Actions
Docker

↓

## OPERATE

Linux
reverse proxy
services
server administration

↓

## SECURE

Authentication
authorization
hardening
application security

Each stage must link to real evidence.

This replaces a generic “How I work” section with something grounded in actual engineering practice.

---

# 12. EXPERIENCE

Keep both internships.

Do not reorder them artificially just to make cybersecurity appear first.

Prefer chronological clarity and role relevance.

For the Application Security internship, verify and use supported facts such as:

* authorized security assessments
* web/API security
* authentication/authorization findings
* code review
* backend/database security review
* collaboration with development teams
* Top 5/100 security competition result

Preserve existing ethical/scope wording.

Do not soften or remove:

“authorized scope”
“responsible disclosure”
and equivalent language.

For the Software Engineering internship:

replace generic wording with verified technical deliverables.

Do NOT invent the actual research project name.

If the repository does not contain enough detail:

{{FILL: exact Software Engineering internship deliverable/project}}

---

# 13. SECURITY BACKGROUND

Add a dedicated but visually secondary section:

# Security Background

The purpose is not to turn the site into a cyber portfolio.

The purpose is to show why the candidate's engineering perspective is unusual.

Where verified, surface:

* 20+ responsible-disclosure / Hall of Fame recognitions
* Bugcrowd / relevant recognition
* DigitalOcean
* Pinterest
* Dell
* Verisign
* Bastion Application Security internship
* Top 5/100 security competition
* security-related education/training

The strongest framing is:

> Security is not a separate interest from my engineering work — it changes how I think about software, APIs and infrastructure.

Do not use the security section as a giant badge wall.

Add:

“Explore my security research →”

linking to:

https://mahmoudouf.com

---

# 14. CAREER / ENGINEERING TIMELINE

Add a lightweight timeline called:

# The Engineering Path

Use only verified events.

Conceptual progression:

Security research / systems curiosity
↓
Software Engineering education
↓
Application Security internship
↓
Software Engineering internship
↓
Software + infrastructure + DevOps career

Do not fabricate years.

Do not claim professional security employment continuously since the earliest research date.

Use early security recognition as independent research/history, not fake employment.

---

# 15. SKILLS

Replace the huge flat list with evidence-oriented tiers.

Suggested:

## Core

Technologies repeatedly used in substantial projects.

## Applied

Technologies used in internships or meaningful projects.

## Exposure

Technologies studied or used at smaller scope.

The actual classification MUST be derived from repository evidence.

Do not call something “Core” just because it appears on the résumé.

Do not display fake percentages or progress bars.

Keep the total visible list around 15–20 high-signal items.

Prioritize:

* Python
* Django
* FastAPI
* PostgreSQL
* Linux
* Docker
* Git
* GitHub Actions
* CI/CD
* Nginx
* WebSockets
* JavaScript
* C#/.NET where supported
* networking
* application security

Kubernetes/AWS/etc. must appear only at the level supported by actual evidence.

---

# 16. TECHNICAL WRITING

Keep the existing real articles.

Current topics include:

* GitHub Actions / CI/CD
* Docker Compose
* Linux server security

Strengthen titles/descriptions for clarity.

Do NOT publish fictional articles.

Do NOT make outlines look like existing articles.

Optionally create a separate clearly labeled section:

“Next technical topics”

but only if useful.

Potential future topics may include:

* securing a Django API from lessons learned in security testing
* deploying and hardening a Python application
* operating self-hosted services on constrained infrastructure
* lessons from building real-time collaborative systems

These are recommendations, not fabricated publications.

---

# 17. PROJECT PRIORITY

Use project hierarchy instead of six equal cards.

Featured projects should demonstrate:

### Software engineering

TicketPro / Team Work Platform

### DevOps

CI/CD Pipeline

### Systems

ALBrowser / Linux packaging

### Real infrastructure

zrun.dev self-hosted architecture

This gives recruiters different proof paths.

---

# 18. RECRUITER FAST SCAN

The first screen should communicate:

MAHMOUD ADEL

Security-minded Software Engineer

DevOps · Infrastructure · Application Security

10+ engineering projects
2 technical internships
Top 5 / 100 security competition
BSc Software Engineering

[Download Résumé]
[View GitHub]

Do not require scrolling to figure out what job category the candidate belongs to.

---

# 19. CROSS-LINK THE TWO PORTFOLIOS

At the end of zrun.dev, show:

## Security background

20+ responsible-disclosure / Hall of Fame recognitions
Application Security internship
Top 5 / 100 Bastion security competition

[Explore security research →]

Link to:

https://mahmoudouf.com

At mahmoudouf.com, provide the reciprocal developer/engineering link where appropriate:

[Engineering portfolio → zrun.dev]

The two sites should feel like:

zrun.dev
= software + infrastructure + operations

mahmoudouf.com
= security + research + offensive/defensive engineering

---

# 20. FOOTER

Use a concise closing CTA.

Preferred direction:

> Build it. Ship it. Secure it.

or:

> Let's build systems that work — and understand why they fail.

Then:

GitHub
LinkedIn
Email
Résumé
Engineering portfolio
Security portfolio

No contact form unless one already exists and is useful.

---

# 21. SEO / META

Update:

<title>

toward:

Mahmoud Adel — Security-minded Software Engineer

or a concise equivalent.

Meta description should include:

* security-minded software engineer
* Linux / Docker / DevOps
* concrete evidence

Example:

“Mahmoud Adel is a security-minded software engineer working across Python, Linux, Docker, CI/CD and application security.”

Verify final length ≤155 characters.

Add / correct:

* og:title
* og:description
* og:image
* canonical URL
* favicon

Do not create fake social images.

Use existing assets where available or insert:

{{FILL: Open Graph image}}

---

# 22. ACCESSIBILITY / PERFORMANCE

Do not add heavy dependencies.

Preserve or improve:

* keyboard accessibility
* semantic headings
* alt text
* focus states
* reduced-motion support
* mobile layouts
* fast loading

The graph must degrade gracefully to list view on mobile.

Do not use animation purely for decoration.

Animations should communicate:

* relationships
* data flow
* architecture
* evidence connections

---

# 23. VISUAL DIRECTION

Preserve the existing visual identity.

Do NOT convert the site into:

* cyberpunk
* Matrix aesthetic
* glowing hacker terminal theme
* generic SaaS template
* excessive glassmorphism
* giant badge wall
* giant technology logo wall

The desired feeling is:

premium engineering portfolio

* technical control center
* calm professional product design

The interactive graph should remain the visual signature.

---

# 24. CLAIM-SAFETY PASS

Before finalizing, scan the entire site for:

* expert
* enterprise-grade
* production-grade
* scalable
* world-class
* revolutionary
* passionate
* fast learner
* any unsupported metric
* any unsupported production claim
* any unsupported cloud claim
* any fabricated security result

Replace or remove them.

Every project must answer:

What was the problem?
What did I build?
What technologies did I use?
What interesting engineering decision did I make?
What security consideration exists?
What operational/automation element exists?
Where is the evidence?

---

# 25. PLACEHOLDER POLICY

Whenever information is genuinely missing, insert:

{{FILL: exact information required}}

Do not invent it.

At the end provide a complete table of every placeholder.

Examples:

{{FILL: exact Software Engineering internship deliverable}}
{{FILL: verified VPS architecture}}
{{FILL: live demo URL}}
{{FILL: project screenshot}}
{{FILL: résumé PDF path}}
{{FILL: Open Graph image}}

---

# 26. FINAL VALIDATION

Before completion:

* run the project's build command
* fix all build errors
* verify all internal links
* verify GitHub links
* verify external portfolio links
* verify résumé link
* verify mobile layout
* verify accessibility
* verify metadata
* verify no broken images
* verify no placeholder text except explicit {{FILL: ...}}
* verify the capability graph
* verify list/recruiter mode
* verify reduced-motion behavior

---

# FINAL DELIVERABLE

Provide:

1. Changed files and one-line explanation for each.
2. Before/after hero copy.
3. Before/after one flagship project card.
4. All new sections added.
5. All claims that were deliberately softened/removed because they were too strong.
6. Complete list of {{FILL: ...}} placeholders.
7. Build/test result.
8. Any factual inconsistencies found between the two sites.
9. A short recruiter-readout describing what impression the final homepage should create.

Do not merely make the portfolio look more impressive.

Make the candidate's existing evidence easier to understand, harder to dismiss, and more relevant to Software Engineering, DevOps, DevSecOps and Systems roles.

The desired final recruiter reaction is:

“This person is early-career, but unusually hands-on. He builds software, understands Linux and infrastructure, has actually worked with security, and can explain what he built. I want to interview him.”

---

# APPENDIX — Visual & Design Bug Fixes, Implementation Learnings from portfolio-infosec
*Targeting: `portofolio-dev-new` — forked codebase. All paths below are relative to `/home/mahmoudadel/portofolio-dev-new/` (mirrored from `portfolio-infosec`). A future agent must implement MoE core above PLUS these fixes as pre-conditions, in order given. Paths are target-dir only.*

> **Read this first before any MoE section implementation. These are hard-won visual regressions and why they happened.**

## 0. PWA Top Bar — Do It Right First (Critical, Mobile Breaks Everything)

*Lesson from infosec: top bar broke 3 times before native 56px was stable. Do it once correctly here.*

**Wrong approach seen:** `container-fluid mx-sm-4` gutters + `position:relative` header + `height` derived from `padding:1rem` + `showHeaderOnTop` toggling `position:sticky` only on scroll. Result: header transparent at `scroll==0`, hero `clamp(4.5rem)` underlaps, right cluster `346px` overflows `320vw`, hamburger `24px` <44px, `grid 44px 1fr auto` mis-places socials into middle column when collapse is `position:absolute`.

**Correct PWA spec (implement in `static/css/design.css` + `static/css/recruiter.css`, do NOT touch `layouts/partials/sections/header.html` mx-* — override via CSS):**

```css
#profileHeader{
  position:sticky; top:0; z-index:1000;
  height:56px; min-height:56px;
  padding:env(safe-area-inset-top) env(safe-area-inset-left) env(safe-area-inset-right) 0;
  background-color:var(--bg) !important; /* fallback */
  background:color-mix(in srgb, var(--bg, var(--recruiter-bg, #f5f4f0)) 92%, transparent) !important;
  backdrop-filter:blur(14px) saturate(120%); -webkit-backdrop-filter:blur(14px);
  border-bottom:1px solid var(--border, var(--recruiter-border));
  isolation:isolate;
}
#profileHeader nav.navbar{ height:56px; min-height:56px; padding:0 !important; display:flex; align-items:center; background:transparent !important; }
#profileHeader .container-fluid{
  height:56px; width:min(1180px, calc(100% - 2rem)); margin-inline:auto !important;
  display:grid; grid-template-columns:44px 1fr auto; gap:0.75rem; align-items:center;
}
@media(max-width:991px){ #profileHeader .container-fluid{ width:min(100% - 1.5rem,620px) } }
@media(max-width:480px){ #profileHeader .container-fluid{ width:min(100% - 1.25rem,620px); gap:0.35rem } }
#profileHeader .navbar-toggler{ width:44px; height:44px; display:grid; place-items:center; border:1px solid var(--border); border-radius:10px; }
.nav-social-links, #profileHeader .nav-social-links{
  display:flex !important; flex-direction:row !important; flex-wrap:nowrap !important;
  justify-content:flex-end !important; align-items:center !important;
  gap:0.15rem !important; overflow:hidden; max-width:100%; grid-column:3; justify-self:end;
}
.nav-social-icon, .nav-theme-btn, #theme-toggle{ width:44px; height:44px; min-width:44px; }
#profileHeader .nav-resume-link{ min-height:36px; padding:0.35rem 0.85rem; font-size:0.78rem; white-space:nowrap; }
#profileHeader .navbar-toggler:focus-visible, #theme-toggle:focus-visible{ outline:3px solid var(--recruiter-accent); outline-offset:3px; }
#profileHeader .navbar-collapse{ position:absolute; top:100%; left:0; right:0; width:100%; background:color-mix(in srgb, var(--bg) 98%, transparent); backdrop-filter:blur(12px); border-bottom:1px solid var(--border); z-index:999; }
@media(min-width:992px){ #profileHeader .navbar-toggler{display:none !important} #profileHeader .container-fluid{ grid-template-columns:1fr auto } #profileHeader .nav-social-links{ grid-column:2 } }
@media(max-width:360px){ #profileHeader .nav-social-links .nav-item:nth-child(4){display:none} .nav-social-icon{width:36px;height:36px} }
section[id]{ scroll-margin-top:calc(56px + env(safe-area-inset-top)) }
.hero-recruiter-shell, .recruiter-section{ scroll-margin-top:calc(56px + env(safe-area-inset-top)) }
```

*Also patch `showHeaderOnTop` duplicate in `design.css:1083` to same `color-mix 92% !important` to avoid jump, and `html body #profileHeader` specificity hardening to beat `body.light > #profileHeader`.*

*Why:* `mx-*` leftover + `color-mix` without `!important` + missing `safe-area` + `scroll-margin 72px` (old) caused screenshot overlap (hamburger + OPEN TO WORK + pill + Résumé + socials all at y:0). Fixed once, never broke again.

---

## 1. Blog Shell Clipping — Decoupling from Bootstrap

**Files:** `layouts/_default/single.html:58`, `static/css/blog.css:85-130`

*Bug:* `class="blog-shell container"` collided: Bootstrap `--bs-gutter-x` + padding + `width:min(1160px,calc(100%-2rem))` clipped left headings `h2::before at -1rem` and terminal code blocks `pre{margin:5px}` from `themes/single.css`.

*Fix (target: `portofolio-dev-new/layouts/_default/single.html` + `static/css/blog.css`):*
- Remove `container` class: `<div class="blog-shell">` only.
- `.blog-shell{ box-sizing:border-box; padding-left/right:0; --bs-gutter-x:0; width:min(1160px,calc(100%-2rem)); max-width:none!important; margin-inline:auto; }` + `.blog-shell.container{all:unset}` safety.
- `.blog-layout/.blog-main-column/.blog-prose{ min-width:0; box-sizing:border-box }` + `overflow:visible` on header/prose so `h2::before left:-1rem` not clipped.
- Code blocks: `>pre, >.highlight, .term-block{ max-width:100%; box-sizing:border-box; margin-inline:0; overflow-x:auto; }` and neutralize legacy `pre{margin:5px}` via `#single.blog-page .page-content pre{margin:0!important; margin-block:1.6rem 2rem!important}`.
- **Deeper fix:** Later removed self-defeating `html{overflow-x:clip}`, `#single{overflow-x:clip}`, `.blog-shell{overflow-x:clip}` chain — children `overflow:visible` can never escape parent clip. Rehoused `h2::before{left:-1rem → left:0}` and added `h2{padding-left:1.2rem}` + `.blog-prose{padding-inline:0.5rem}`.

*Lesson:* Never use `overflow-x:clip` on html/body for wash containment — radial `::before` at `z-index -2` + `isolation:isolate` is enough; clip chain hides both decoration and list `1. USE`.

---

## 2. Blog Background — Gravity Atlas Washes, Not Grid

**File:** `static/css/blog.css`

*Before:* `44px linear grid + repeating-linear pinstripe ::after` — cheap notebook look, broke parity with hero `recruiter.css` soft radials.
*After:* Two `radial-gradient(circle at 16% 8% rgba(16,185,129,0.08) 32rem, 86% 16% rgba(99,102,241,0.08) 30rem)`, `background-size:auto`, `::after{content:none}`, `--blog-bg var(--bg,#f5f4f0) light / #0b1020 dark`.

---

## 3. Term Block Unclosed DIV

**File:** `layouts/_default/_markup/render-codeblock.html:49`

Template opened `<div class="term-block">` at line 31 but closed only 5 of 6 divs (term-bar, term-body, term-console + 2 inner). Following `h2`/`p` parsed inside frame, clipped by `overflow:hidden`. Fix: add missing `</div>` after `{{- end }}` before `{{- else -}}` — closes outer frame for both interactive and non-interactive `if $isShell`. Byte-identical fix to both repos, no CSS.

---

## 4. Portfolio Graph — Keep DOM/SVG, Don't Swap Canvas

**Why kept:** Canvas engine can't render FA SVG-injection per node without HTML→canvas pipeline; DOM/SVG keeps `cnode--*` positions, list view, hire-me popup, terminal `cterm-*` (which reuses `terminal.css`), panel signature.

**Files:** `static/css/portfolio-graph.css`, `static/css/portfolio-panel.css`, `layouts/index.html`, `layouts/partials/sections/hero/index.html`, `static/js/portfolio-graph-data.js`, `static/js/portfolio-graph.js`, `static/js/graph-background.js`

**Reskin tokens (target dir):** `--cg-*` palette (indigo `#6366f1`/green `#10b981`/teal `#14b8a6`/blue `#3b82f6`/gray `#6b7280`/slate `#64748b`/violet `#8b5cf6`/pink `#db2777`/amber `#f59e0b`/sky `#0ea5e9` light/dark), glass `rgba + sheen 999px/16px`, Space Grotesk + Courier-mono, `--lg-*` legend tokens, `#graph-bg-canvas z-index 0 < svg 2 < nodes 5`.

**JS side contract (must match CSS names):** `window.GraphBackground.init/destroy` mounts `canvas#graph-bg-canvas` as first child of `.constellation-wrap` (radial wash, fog blobs, 140/60 stars, particles, vignette, indigo glow tracking me-node, seed-42 LCG). Edges: `line → quadratic path` with same classes/metadata so `edgeLookup` + GSAP intact; lit edges `getPointAtLength` flow dots `filter:drop-shadow`. Hover emits `is-hover/is-dim/is-lit` + legacy `cnode--active/dimmed`, float card `.graph-float-card (.gfc-title/.gfc-badges/.gfc-link)` cursor-follows, hidden on coarse pointer/list. Palette `CAT_COLORS/GROUP_COLORS` must match `--cg-*` exactly (or `color-mix` never sees new hues). Fix separator glyph `|` → `·` and term `cterm-sep` padding `0.1em → 0.35em opacity .55` at higher specificity than `terminal.css`.

**Implementation issue faced:** Two parallel agents (CSS + JS) left palette gap — CSS expected new hues, JS shipped old — caught only by visual `color-mix` never tinting. Fix required version bumps `customScripts` `v5→6/6→7` cache bust.

---

## 5. Legend Overflow — Flow Below Graph, Not Overlay

**Before:** `.constellation-legend{position:absolute bottom:2rem left:50% translateX(-50%)}` sat on nodes at `y~93%`, `wrap padding 2rem 1rem 4rem`.

**After (target: `portofolio-dev-new/static/css/portfolio-graph.css:77-90, 967-985`):**
- `.constellation-wrap{ padding:2rem 1rem 2.5rem; gap:2.2rem; flex column; }` (`+0.8rem` extra safe after still colliding → helper phrase removed + `legend margin 2rem`)
- `.constellation-legend{ position:relative; bottom:auto; left:auto; transform:none; margin:2rem auto 0; }`
- `#portfolio-constellation{ margin-bottom:0.8rem }`
- `@991` remove redundant absolute override.

*Second pass:* Helper phrase `Click a node for evidence · switch to list view` at `hero-graph-intro` removed — it collided with legend even after flow fix; kept `Switch to List View` button only + added extra `wrap gap` `1.2→2.2rem`.

---

## 6. Skill Wall — Single Source, No Scale Illusion

**Bug:** First 3 pills looked larger. Cause: (a) competing `custom.css`/`design.css` flex `1.0rem` vs `recruiter.css` grid `1.02rem`, (b) missing `stretch` → asymmetric cell fill, (c) GSAP `scale 0.7 + back.out(1.4) stagger 0.04` animates first items to `1.0` earlier while others still scaled.

**Fix (target: `portofolio-dev-new/static/css/{recruiter,custom,design}.css`, `static/js/scroll-animations.js`):**
- Delete all `#about .skills-list` from `custom.css` (flex+1.0rem) and `design.css:413-451` (replace with comment), keep only `#awards` in `custom.css`.
- Harden `recruiter.css` as single source: `display:grid !important; grid-template-columns:repeat(3,minmax(0,1fr)) !important; gap:0.70rem !important; justify-items:stretch !important; align-items:stretch !important; .skill-item/.badge width:100% !important; justify-content:flex-start !important; @767/480 bump to !important;`
- GSAP `scroll-animations.js`: `scale 0.7 + back.out(1.4)` → `opacity-only ease power2.out + clearProps:'transform'`.

*Later passes:* Toolkit pills enlarged `0.82→1.02rem pad 0.5×0.65→0.62×0.95 gap 0.55 border 0.62→0.75 weight 500→600 marker 0.38→0.46` (byte-identical), skill nodes chip enlarged desktop `0.92→1.05rem pad 0.45×0.95 icon 30→34`, JS `minDists.skill 150→175 desktop, 90→105 mobile, spread 120→135`.

---

## 7. Where I Contribute — Too Small + Cutout Overflow

**Bug:** Pills `0.70rem pad 0.2×0.4 radius 0.35 gap 0.35` small vs toolkit `0.78rem/600 0.32×0.6 gap 0.40 radius 0.5` + scaffold screenshot: `Vulnera Assessn / Window: / System Administr n` clipped at card edge. Cause: flex pill `min-width:auto` no `max-width:100%` → intrinsic ~210px exceeds card inner (~285-360px at 3/2-col).

**Fix Option A (keep, enlarge):** `focus-area-grid gap 0.8→0.95rem + stretch/box-sizing`, `card pad 1→1.3rem radius 0.85`, `h4 0.98→1.18rem !important 700`, `items gap 0.35→0.45rem + li 0.88rem !important 600 pad 0.42×0.75 radius 0.5 box-sizing`, `i/svg 1rem line-height 1 inline-block`.

**Fix for cutout (second pass):** Add to `li`: `max-width:100% !important; min-width:0 !important; white-space:normal !important; overflow-wrap:anywhere !important; word-break:break-word` + `align-content:flex-start` + narrow `design.css:902 #about ul → #about .about-content ul` to avoid leaking 2-col grid onto flex pills. Keeps `3→2→1fr`.

*Alternative considered:* Hide `Where I contribute` when `toolkitTiers` exists (single source) — rejected per Option A keep-and-enlarge.

---

## 8. Timeline Gap & Awards Duplication

**Cap between April 2019 DigitalOcean → May 2024 Bastion (5y1m)** — education `Sep 2022-Jul 2026` + certs `2023-24` not shown → recruiter sees hole.

*Added distinguished muted entry at `track-record.html:110` after DigitalOcean, before Bastion:* `timeline-item--gap timeline-item--muted` with `fa-graduation-cap` badge `Education & Growth`, muted `var(--recruiter-muted)` dot, dashed `repeating-linear-gradient` line, dashed border card `background 55% opacity .96`, text `By 2019 I was still in school. The following years were focused on education — B.Sc. Sep 2022-Jul 2026 — and labs/certs (CCNA 2023 · IBM CTI 2023 · SecurityBlue 2023 · TryHackMe 2024 · CompTIA Linux+ 2024). This period also involved personal growth and relocation — challenges that strengthened resilience... Always open to connect.` + links `#education`/`#contact`. Icon beside company title, not blank.

**Awards duplicated track-record (both timelines 6 HoF).** Kept track-record as primary `7-item` chronological evidence timeline. Rewrote `awards.html:1-35` (timeline) → `recruiter-section recruiter-awards` grid wall `recruiter-container + kicker/dek + awards-grid 3-col →2 @991 →1 @767, award-card--featured Bastion Top5, badge/date/h3/p, footer #track-record + Bugcrowd`. CSS `recruiter.css` `color-mix` tokens, integrated breakpoints.

**Skill wall dedup:** Consolidated flat 39 `about.skills` + 32 `toolkitTiers` (7 missing `Exploit Development, Red Team, Firewall, Malware Analysis, Threat Intelligence, Presentation Skills, Problem Solving`) → delete `if .Site.Params.about.skills.enable` flat block in `about.html`, expand tiers `CORE 8→10, APPLIED 8→9, FAMILIAR 8→10, ENGINEERING 8→10` (description → `Programming, systems, infrastructure and professional effectiveness`), keep `writing-count-note` outside.

---

## 9. Homepage Sections & Responsive PWA Symmetry

**New sections added:** SECURITY TRACK RECORD / RESPONSIBLE DISCLOSURE (Bugcrowd absolute) / BREAK BUILD OPERATE (BREAK Web/Network/Vuln/PrivEsc/Disclosure, BUILD Python/JS/C/C++/SQL/Git/tooling/automation, OPERATE Linux/Windows/Networking/Docker/hardening/sysadm) / SELECTED SECURITY PROOF (AppArmor feat + Verisign $1K CSRF→ATO OAuth state + Captcha + Bastion CTF + Disclosure → later 5th centered orphan via `last-child:nth-child(odd){grid-column:1/-1; max-width:calc(50% - 0.5rem); margin:auto}` + later 2 vertical featured AppArmor + linux trial `writing-featured-grid 2-col` ) / RESEARCH & NOTES featured AppArmor sequence `RCE PATH ↓ RUNTIME CONTAINMENT ↓ APPARMOR ↓ ROOT-CAUSE` + dynamic `len` archive `7+ vs 10` fix + Writing count note.

**CSS appended:** `recruiter.css` BBO `3-col #break-build-operate recruiter-bbo`, proof `2-col #security-proof recruiter-proof` (later `last-child` orphan), writing `writing-card--featured` + sequence, toolkit tiers `toolkit-tier` `border-left 3px accent`, disclosure chips, track-record link — all `color-mix` tokens, `72%` bg, `991 2-col 767 1-col`. `portfolio-graph.css` recruiter mode `portfolio-recruiter-summary`, `graph-recruiter-controls`, `graph-mode-btn`, `recruiter-mode-active` (later removed per toggle deletion), `recruiter-summary-grid 4→2→1`. BBO/proof duplicate `4-col` proof block deleted, `bbo background 72%` corrected; `portfolio-graph.css:1606` bbo leak removed.

**PWA symmetry hardening (991/767/480):** `recruiter.css` at `991` writing/credentials/projects/focus/about-proof `2col gap 0.9rem` with `min-width:0/max-width:100%/box-sizing`; `767` all grids → `1fr gap 0.8rem width 100% align-items:stretch`, hero shell `justify-items:center` + `margin-inline:auto`, hero identity `justify-self:center`, hero-graph-intro `centered column`, `recruiter-section padding 3rem 0` `heading/dek centered`, graph controls `flex-wrap centered 620px`, timeline `width100% overflow-x hidden overflow-wrap:anywhere`; `480` `1fr !important width100%!important` + container `min(100% -1.25rem,620px)`, `min-width0/max-width100%`. Later refined 56px sticky symmetry (below).

**Where I contribute second pass + blog back link + title expansion:** Blog reward figure enlarged from markdown to `figure.verisign-reward-figure` HTML with `width:100%` + figcaption glass `blog.css`; password-change business logic added; linux post `linux-trial-...md` title `Linux Trial Reset ...` → `Linux Third-Party Desktop Software Trial Bypass Vulnerability` (61ch, slug kept), added Scope simulation note + `Business Logic & Filesystem Trust` subsection (WAN IP business logic error, server account validation, Windows registry vs Linux bind).

---

## 10. SEO & OG — One Copy Only

**Wrong:** `layouts/index.html` duplicated `og:title/description/type/url` already emitted by `_internal/opengraph + twitter_cards` → scraper confusion + middle redundant `twitter:card` vs `summary_large_image`.

**Fix:** Keep only non-duplicate extras in `index.html:2-12`: `og:image + twitter:image via absURL`, `twitter:card summary_large_image`, `canonical via .Permalink` fallback. Internal templates handle title/description/type/url. Also add `keywords` meta `hugo.yaml:params.keywords` `298-char taxonomy` + `Person` JSON-LD `knowsAbout/sameAs` + `BlogPosting` JSON-LD `headline/description/author/dates/image/url/mainEntityOfPage` in `single.html:5-20` (with `plainify`/`truncate` + `absURL`).

*Description crafted to 165ch:* `Junior Penetration Tester — authorized security testing, penetration testing, bug bounty, software engineering, security research, responsible disclosure, OAuth/CSRF` — covers 9 keywords readable, Bastion/HoF stays in `keywords`.

---

## 11. RECRUITER MODE Toggle — Friction → Always Visible

*Initial:* `EXPLORE/RECRUITER` mutually exclusive IIFE `static/js/portfolio-graph.js:293` with `body.recruiter-mode-active`, `is-active/aria-pressed`, `aria-controls`, `Escape` + `listToggle` mutual sync `v10`. Added header OG.

*Reviewed:* Long toggle friction for 60-sec scan, hidden behind button. Removed: delete `graph-recruiter-controls` + `hidden` summary from `hero/index.html:80-98`, delete IIFE, clean `listToggle.applyMode` (remove recruiter sync), CSS `portfolio-graph.css` delete `[hidden]/.recruiter-mode-active/.graph-mode-btn` blocks, ensure `.portfolio-recruiter-summary{display:block}`. Summary now always visible under `constellation-wrap + portfolio-list-view` (persistent anchor).

---

## 12. Mobile Graph Hide — Conditional, Not Brutal

**Wrong initial:** `window.innerWidth <992` one-way, never reverts on grow, mismatch `767` layout, heavy `2600/1400` on touch.

**Correct:** Unify to `767 OR (pointer:coarse)` via `matchMedia('(pointer:coarse)')` + `sessionStorage portfolioView` (list/graph) + `graph-force-mobile` escape. Initial respects `storedView else shouldDefaultList()`. Toggle saves preference, `resize` + `coarseMatcher change` re-force only if `shouldDefaultList() && !listMode && storedView!=='graph'` (respect explicit graph on tablet). CSS failsafe `@media (pointer:coarse) and (max-width:767px){ body:not(.graph-force-mobile) .constellation-wrap{display:none} #portfolio-list-view{display:block} .graph-view-controls{display:none} }` — phones list-only noscript fallback, fine-pointer iPads keep graph. Bump `v10→v11`.

---

## 13. GSAP & Cache Lessons

- `scroll-animations.js`: `scale 0.7` stagger `0.04` was the *visual* pill-size bug — not CSS. Fixed to `opacity-only power2.out + clearProps:'transform'`.
- `customScripts` version bumps `v5→6/7/9→10→11` mandatory for palette/glyph/cache — pattern established, always bump on JS/palette change.

---

## 14. Content Truth — Recruiter 60-Sec Checklist

*Before finalize scan every claim:* lab ≠ client, training ≠ cert, `critical` only where source supports (DigitalOcean), no invented CVE/bounty/scale. Implementation checklist in MoE §24-26 covers `expert/scalable/world-class`.

---

